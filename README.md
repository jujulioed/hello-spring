After opening the app on your IDE and let it synchronize: 

# How to Build?

With java 17 and gradle installed **on windows**, run:

```
gradlew build
```

# How to run?

Run this on project folder:
```
java -jar build/libs/greetingspring-0.0.1-SNAPSHOT.jar
```